# ===========================================
# Docker Compose Environment Variables
# ===========================================
# Copy this file to .env and adjust values as needed
# Usage: cp .env.example .env

# ===========================================
# PostgreSQL Configuration
# ===========================================
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres_secure_password
TICKETING_DB_NAME=ticketing_db

# ===========================================
# Redis Configuration
# ===========================================
REDIS_PASSWORD=redis_secure_password

# ===========================================
# Backend Ticketing Service
# ===========================================
ENV=development

# Database Connection
DB_MAX_OPEN_CONNS=25
DB_MAX_IDLE_CONNS=5
DB_CONN_MAX_LIFETIME=5m

# JWT Configuration
JWT_SECRET=dev-secret-key-change-in-production
JWT_EXPIRY=24h

# Object Storage
STORAGE_TYPE=local
S3_BUCKET=ticketing-assets
S3_REGION=us-east-1

# External Services (Stubbed)
PAYMENT_GATEWAY_KEY=stub
EMAIL_SERVICE_KEY=stub

# CORS
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# ===========================================
# Notes:
# ===========================================
# 1. Docker Compose will automatically load .env file from the same directory
# 2. Variables use ${VAR_NAME:-default_value} syntax in docker-compose.yaml
# 3. If variable is not set in .env, the default value after :- will be used
# 4. For production, change all passwords and secrets!